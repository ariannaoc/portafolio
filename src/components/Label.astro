---
interface Props {
    title: string;
    color: string;
}

const {title, color} = Astro.props;
---

<div class=`rounded-full px-4 py-2 font-semibold w-fit ${color} bg-opacity-80 shadow-md text-xs sm:text-base`>
    {title}
</div>